// init.js - AOS initialization

// ========== INITIALIZE AOS ==========
if (typeof AOS !== 'undefined') {
    AOS.init({
        duration: 600,
        once: true,
        offset: 80,
        easing: 'ease-out'
    });
}
